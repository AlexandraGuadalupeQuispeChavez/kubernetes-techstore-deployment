apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: techstore-app
  labels:
    app: techstore
    tier: database
spec:
  type: ClusterIP
  selector:
    app: techstore
    tier: database
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
